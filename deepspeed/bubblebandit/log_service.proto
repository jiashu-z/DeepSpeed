syntax = "proto3";

service LogServer {
  // Write log entry.
  rpc WriteLog (LogEntry) returns (Empty) {}
}

message LogEntry {
  int64 pid = 1;
  int64 ts = 2;
  string msg = 3;
}

message Empty {

}